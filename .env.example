# Copy this file to .env and fill in your key for local testing with `vercel dev`.
# In production, set this in your Vercel project's Environment Variables instead —
# never commit a real API key to source control.
ANTHROPIC_API_KEY=sk-ant-your-key-here
